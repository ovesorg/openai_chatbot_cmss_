apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: chatbot
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/ovesorg/openai_chatbot_cmss_.git'
    targetRevision: fastapi
    path: charts/templates
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: chatbot-namespace
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
