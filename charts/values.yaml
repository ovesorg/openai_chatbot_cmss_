replicaCount: 1

image:
  repository: ghcr.io/ovesorg/chatbot
  tag: latest
  pullPolicy: IfNotPresent

imagePullSecrets:
  - name: ghcr-secret-oves

service:
  type: LoadBalancer
  port: 80

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""


ingress:
  enabled: true
  className: ""
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/issuer: "letsencrypt-dev"
    kubernetes.io/tls-acme: "true"
  hosts:
    - host: dev-chatbot.omnivoltaic.com
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls:
    - secretName: chatbot-tls
      hosts:
        - dev-chatbot.omnivoltaic.com

resources:
  requests:
    cpu: "250m"
    memory: "256Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"

nodeSelector: {}

tolerations: []

affinity: {}

openaiApiKey: "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"
pineconeApiKey: "199b3561-863a-41a7-adfb-db5f55e505ac"

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80
  targetMemoryUtilizationPercentage: 80
